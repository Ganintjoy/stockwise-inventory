PORT=3000
NODE_ENV=production
# sqlite (default) or postgres
DB_CLIENT=sqlite
SQLITE_FILE=./data/stockwise.db
# Required only when DB_CLIENT=postgres
DATABASE_URL=postgresql://stockwise:change-me@localhost:5432/stockwise
# Set true for hosted PostgreSQL services that require SSL
DATABASE_SSL=false
